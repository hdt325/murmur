{
  "name": "murmur",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "npx tsx server.ts",
    "test": "npx tsx tests/test-smoke.ts",
    "test:e2e": "npx tsx tests/test-e2e.ts",
    "test:bugs": "npx tsx tests/test-bugs.ts",
    "electron": "cd electron && npx electron .",
    "build:mac": "cd electron && npx electron-builder --mac",
    "build:win": "cd electron && npx electron-builder --win"
  },
  "dependencies": {
    "chokidar": "^4.0.3",
    "express": "^4.21.0",
    "ws": "^8.18.0"
  },
  "optionalDependencies": {
    "node-pty": "^1.0.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.0.0",
    "@types/ws": "^8.5.12",
    "playwright": "^1.58.2",
    "tsx": "^4.19.0",
    "typescript": "^5.5.0"
  }
}
